{% extends "app/layout.html.twig" %}

{% block content %}
    <main class="flex-1">
        <div class="py-6">
            <div class="max-w-7xl mx-auto px-4 sm:px-6 md:px-8">
                <h1 class="text-2xl font-semibold text-gray-900">Tracker</h1>
            </div>
            <div class="max-w-7xl mx-auto px-4 sm:px-6 md:px-8">

{#                <div>#}
{#                    <div class="col-span-6 sm:col-span-3">#}
{#                        <label for="country" class="block text-sm font-medium text-gray-700">Group by</label>#}
{#                        <select id="country" name="country" autocomplete="country-name" class="mt-1 block w-full bg-white border border-gray-300 rounded-md shadow-sm py-2 px-3 focus:outline-none focus:ring-indigo-500 focus:border-indigo-500 sm:text-sm">#}
{#                            <option>Habit</option>#}
{#                            <option>Category</option>#}
{#                        </select>#}
{#                    </div>#}
{#                </div>#}

                <div class="max-w-lg mx-auto pt-8">
                    {% for date, tracker in days %}
                        <h2 class="max-w-6xl mx-auto mt-8 px-4 text-lg leading-6 font-medium text-gray-900 sm:px-6 lg:px-8 text-center">{{ date }}</h2>


                        <ul role="list" class="divide-y divide-gray-200">
                            {% for habit_name, value in tracker %}
                                <li class="py-4">
                                    <div class="flex space-x-3">
                                        <div class="flex-1 space-y-1">
                                            <div class="flex items-center justify-between">
                                                <h3 class="text-sm font-medium">{{ habit_name }}</h3>
                                            </div>

                                            <div class="flex items-center justify-between">
                                                <p class="text-sm text-gray-500">{{ value }}</p>
                                            </div>
                                        </div>
                                    </div>
                                </li>
                            {% endfor %}
                        </ul>
                    {% endfor %}
                </div>
            </div>
        </div>
    </main>
{% endblock %}